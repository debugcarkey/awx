- name: Hello World Sample
  hosts: all
  roles:
     - { role: tommarshall.awscli }
  tasks:
    - name: Hello Message
      debug:
        msg: "Hello World! {{ stack_name }}"
    - name: Create a k8s namespace
      kubernetes.core.k8s:
        name: testing
        api_version: v1
        kind: Namespace
        state: present
